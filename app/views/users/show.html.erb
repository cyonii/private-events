<div class="mt-5">
  <h1 class="text-muted">Hello, <%= @user.name %></h1>
</div>

<div class="row">


  <% for event in @user.events %>
  <div class="col-sm-6 col-md-4 col-lg-3">
    <%= render 'events/event_card', :event => event %>
  </div>
  <% end %>
</div>
