<h1 class="fs-5 fw-bold text-primary">Events</h1>

<div class="row">
  <% for event in @events %>
  <div class="col-sm-6 col-md-4 col-lg-3 mb-3">
    <%= render 'event_card', :event => event %>
  </div>
  <% end %>
</div>
